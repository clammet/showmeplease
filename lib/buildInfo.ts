// The Docker build overwrites this file with the commit it was built from
// before bundling, so both the static frontend and the backend bundle carry
// it. The checked-in empty value marks a development build.
export const GIT_COMMIT = "";
